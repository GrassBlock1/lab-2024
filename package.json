{
	"scripts": {
		"build": "hugo",
		"elog:init": "elog init",
		"sync:local": "elog sync -e .elog.env",
		"sync": "elog sync",
		"clean": "elog clean"
	},
	"dependencies": {
		"@elog/cli": "^0.13.2"
	},
	"devDependencies": {
		"markdownlint": "^0.34.0",
		"markdownlint-cli2": "^0.13.0"
	}
}
